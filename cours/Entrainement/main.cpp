/*

Notez que l'on parle de "dynamisme" en C++ pour qualifier une action réalisée pendant
l'exécution de votre programme. Une conversion de types dynamique est donc une
conversion qui va s'effectuer pendant l'exécution de votre programme et non par le
compilateur comme c'est le cas pour les trois autres cast existants.


Il est également important de savoir qu'un pointeur (ou une référence) constant(e)
n'est pas un pointeur ou une référence dont on ne peut modifier la valeur mais dont
l'élément pointé ne peut être modifié en passant par ce pointeur (ou cette référence)

Contrairement au C, les cast C++ sont effectués au moyen d'opérateurs spécifiques (mots-clés réservés).
Le choix de ces opérateurs dépend du type de cast, car le C++ différencie quatre types de conversion explicite.
Tout d'abord, trois opérateurs sont dédiés aux conversions statiques (effectuées à la compilation) :

*/

# include <string>
# include <iostream>

int main() {
	

}
