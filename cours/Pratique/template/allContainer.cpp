/*
On distingue conteneurs en séquence et conteneurs associatifs

Séquences :
	vector
	deque
	list
	stack
	queue
	priority_queue
Conteneurs associatifs :
	set
	multiset
	map
	multimap

Les conteneurs ont les mêmes noms de méthodes entre eux.

	size()renvoie la taille d'un vector, d'une list ou d'une map
	empty()qui renvoie true si le conteneur est vide et false sinon
	clear() vide entièrement un conteneur
	swap() échange le contenu de deux conteneurs de même type


Il faut surtout savoir utiliser map et vector

*/
